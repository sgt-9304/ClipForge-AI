export type TimedWord={word:string;start:number;end:number};export type ClipScores={hook:number;clarity:number;specificity:number;value:number;novelty:number;pacing:number;captionReadability:number;ending:number;contextPenalty:number;riskPenalty:number};export type ClipCandidate={title:string;hook:string;startSeconds:number;endSeconds:number;reason:string;scores:ClipScores;predictedEngagementScore:number;reviewStatus:"pending"|"approved"|"rejected"};