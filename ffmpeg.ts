import {spawn} from "node:child_process";function run(bin:string,args:string[]){return new Promise<void>((resolve,reject)=>{const p=spawn(bin,args,{stdio:"inherit"});p.on("exit",c=>c===0?resolve():reject(new Error(`${bin} exited ${c}`)))})}export async function extractAudio(input:string,output:string){await run(process.env.FFMPEG_PATH||"ffmpeg",["-y","-i",input,"-vn","-ac", "1","-ar","16000",output])}export async function renderVertical(input:string,output:string,start:number,duration:number,srt?:string){const vf=["scale=1080:1920:force_original_aspect_ratio=increase","crop=1080:1920"];if(srt)vf.push(`subtitles=${srt}`);await run(process.env.FFMPEG_PATH||"ffmpeg",["-y","-ss",String(start),"-i",input,"-t",String(duration),"-vf",vf.join(","),"-af","loudnorm","-c:v","libx264","-c:a","aac",output])}