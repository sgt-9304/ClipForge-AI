import {createClient} from "@supabase/supabase-js";export function admin(){const u=process.env.NEXT_PUBLIC_SUPABASE_URL,k=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!u||!k)throw new Error("Storage configuration missing");return createClient(u,k,{auth:{persistSession:false}})}export async function upload(bucket:string,path:string,file:File|Blob){const {data,error}=await admin().storage.from(bucket).upload(path,file,{upsert:false});if(error)throw error;return data.path}