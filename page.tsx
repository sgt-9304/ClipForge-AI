"use client";import {useState} from "react";export default function Upload(){const[result,setResult]=useState<any>(null);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const r=await fetch("/api/upload",{method:"POST",body:new FormData(e.currentTarget)});setResult(await r.json())}return <main className="shell"><h1>Upload authorised media</h1><form className="card" onSubmit={submit}><label><input type="checkbox" name="rightsConfirmed" value="true" required/> I confirm I have rights and required consent.</label><input type="file" name="media" accept="video/*,audio/*" required/><button className="button">Upload and queue</button></form>{result&&<pre className="card">{JSON.stringify(result,null,2)}</pre>}</main>}